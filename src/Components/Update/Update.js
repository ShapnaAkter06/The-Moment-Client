import React from 'react';
import useTitle from '../../Hooks/useTitle';

const Update = () => {
    useTitle('Update')
    return (
        <div>
            <h2>Updated Review</h2>
        </div>
    );
};

export default Update;